<%= form_for([@article, @article.sections.build]) do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :body %><br>
    <%= f.cktext_area :body %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>