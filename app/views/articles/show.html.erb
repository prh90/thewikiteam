
<h1><%= @article.title %><%= link_to "Revisions", article_revisions_path(@article) %> </h1>

<p>
    <%= sanitize @article.summary %>
</p>
  <% @images.each do |image| %>
<p>
  <%= sanitize image.image %>
</p>
  <% end %>
<% @sections.each do |section| %>
  <h3><%= section.title %><%= link_to "Edit", edit_section_path(section) %></h3>
  <p>
    <%= sanitize section.body %>
  </p>
<% end %>
<p>
  <%= link_to "Create A Section", new_article_section_path(@article) %>
</p>
<h2>References</h2>
<hr />
<%= sanitize @article.footer %>
